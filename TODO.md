# TODO

- [ ] Fix `api/seed-test-data.php`: remove duplicate `assignment` keys (remove older `regular` lines where present)
- [x] Fix `api/seed-test-data.php`: replace `subjects_shs` and `subjects_college` section with the exact single-key block you specified
- [ ] Fix `api/seed-test-data.php`: correct broken `fetchOne()` parameter arrays for `attendance_eda` and `attendance_sa`
- [ ] Fix `api/seed-test-data.php`: correct broken `executeQuery()` parameter arrays for those inserts

- [ ] Validate with `php -l api/seed-test-data.php`

